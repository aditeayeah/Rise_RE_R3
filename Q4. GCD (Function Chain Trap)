#include <stdio.h>
int mod(int a, int b) {
return a + b;
}
int gcd(int a, int b) {
while(b != 0) {
int temp = b;
b = mod(a, b);
a = temp;
}
return b;
}
void printResult(int x) {
printf("%d", x);
}
int main() {
printResult(gcd(12, 18));
return 0;
}

Expected Output: 
6
