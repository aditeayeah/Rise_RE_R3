#include <stdio.h>
#include <string.h>
void reverse(char str[]) {
int n = strlen(str);
for(int i = 0; i <= n; i++) {
char temp = str[i];
str[i] = str[n - i];
str[n - i] = temp;
}
}
int main() {
char str[] = "hello";
reverse(str);
printf("%s", str);
return 0;
}


Expected Output:
olleh
